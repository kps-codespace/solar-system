name: woflow-learn

on:
    wokflow_dispatch:

jobs:
    context_github:
        runs-on: ubuntu-latest
        steps:
            - name: Github context
              run: |
                   echo ${{tojson(job)}}